# Nav Style Controls

A WordPress plugin providing an admin UI (Appearance → Nav Style Controls) for overriding spacing and colors on the `core/navigation` and `core/navigation-submenu` blocks, for both desktop and mobile/responsive views.

Generates an inline CSS override that takes precedence over the active theme's navigation styles.
